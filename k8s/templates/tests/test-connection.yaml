apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "weekdone-app.fullname" . }}-test-connection"
  labels:
    {{- include "weekdone-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "weekdone-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
