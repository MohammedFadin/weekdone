apiVersion: v1
kind: Service
metadata:
  name: {{ include "weekdone-app.fullname" . }}
  labels:
    {{- include "weekdone-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "weekdone-app.selectorLabels" . | nindent 4 }}
